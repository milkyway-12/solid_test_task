name: solid_test_task
description: "Solid test task project with creating random colors."

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  path_provider: ^2.1.1
  share_plus: ^10.1.4

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  solid_lints: ^0.3.1

flutter:

  uses-material-design: true

